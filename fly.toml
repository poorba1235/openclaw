

app = 'openclaw-agent-demo-aged-dew-1243'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[[mounts]]
  source = 'openclaw_data'
  destination = '/root/.openclaw/workspace'
  initial_size = '1gb'

[http_service]
  internal_port = 18789
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpus = 1
  memory_mb = 1024
